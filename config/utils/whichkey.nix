{
  # TODO: Implement general mappings
  plugins.which-key = {
    enable = true;
    settings = {
      ignore_missing = false;
      icons = {
        breadcrumb = "»";
        group = "+";
        separator = ""; # ➜
      };
      window = {
        border = "none";
        winblend = 0;
      };
    };
    # registrations = {
    #   "<leader>t" = " Terminal";
    # };
  };
}
